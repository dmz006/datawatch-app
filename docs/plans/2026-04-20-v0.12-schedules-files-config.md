# v0.12.0 — Schedules + File picker + Saved commands + Config (read)

- **Date:** 2026-04-20
- **Version at plan time:** v0.11.0
- **Target ship version:** v0.12.0
- **Source of truth:** [`docs/parity-plan.md`](../parity-plan.md) §6 v0.12 +
  re-audit of parent `dmz006/datawatch` `docs/api/openapi.yaml` on the plan
  date.
- **Scope references:** ADR-0005 (1:1 PWA parity), ADR-0019 (scope
  exclusions — raw YAML editor stays blocked).

## Phase status

- **Phase 1** — `:shared` transport (8 methods, 4 domain types, 9 tests) — **shipped `93351fa`**
- **Phase 2** — Schedules UI (Settings card + session-detail menu) — **shipped `cdf01ea`**
- **Phase 3** — File picker (reusable dialog + NewSessionScreen working-dir) — **shipped in this commit**
- **Phase 4** — Saved commands UI — *up next*
- **Phase 5** — Config viewer UI — pending
- **Phase 6** — Session-output backlog pager UI — pending
- **Phase 7** — Docs + release-prep (version bump, tag) — pending

## Summary — original rescope + 2026-04-20 parent catch-up

**2026-04-20 update:** parent shipped v4.0.3 with the 9 endpoints I had
gated (dmz006/datawatch #5–#13 all closed). The deferred list is now
empty from a transport-availability standpoint. v0.12 scope is kept
focused on the originally-planned 5 surfaces because doubling the
release would risk shipping 10 half-finished UIs instead of 5 solid
ones; everything else moves to v0.13 as previously scheduled, but
with **no blocking gate** — mobile just needs to build the UI.

**Original plan — kept in scope for v0.12:**

| PWA surface | Parent endpoint | v0.12 plan |
|---|---|---|
| Schedules CRUD | `GET/POST/DELETE /api/schedule` (singular) | in scope |
| File picker | `GET /api/files?path=` | in scope |
| Saved command library (BL20) | `GET/POST/DELETE /api/commands` | in scope |
| Daemon config viewer | `GET /api/config` (masked) | read-only in scope |
| Session output backlog pager | `GET /api/output` (v0.11 transport) | UI in scope |

**Newly-available but deferred to v0.13** (not blocked on upstream anymore, just scope management):

| Surface | Parent endpoint | Note |
|---|---|---|
| Channels admin | `GET /api/channels`, `PATCH /api/channels/{id}`, `DELETE /api/channels/{id}` | `POST /api/channels` deliberately returns 501 — add-channel routes through `PUT /api/config` per parent design; mobile follows. |
| Test channel send | `POST /api/channel/send` | — |
| Session timeline | `GET /api/sessions/timeline?id=&since=&limit=` | — |
| Per-session model pickers | `GET /api/ollama/models`, `GET /api/openwebui/models` | — |
| Daemon log viewer | `GET /api/logs?level=&since=&limit=` | — |
| Network interfaces | `GET /api/interfaces` | — |
| Daemon restart | `POST /api/restart` | — |
| Config write | `PUT /api/config` (dot-path) | v0.13 — structured form per ADR-0019. |

Signal device linking (BL21) — `/api/link/start` + `/api/link/stream` SSE
+ `/api/link/status` — still BL, candidate for v0.13 with a dedicated
design pass on QR rendering + paired-state persistence.

## Confirmed — v0.11 client-side gates are moot

The "grey out on NotFound" flags in `SessionsViewModel.deleteSupported`
and `ChannelsViewModel.setActiveSupported` default to `true` and only
flip to `false` against a pre-v4.0.3 server. Against current parent
they stay on and the UI just works. No v0.11 code change needed.

## Goal

Ship the four confirmed surfaces + the backlog pager UI with clean,
idiomatic Material 3 patterns — no greyed-out placeholders for the
deferred items.

## Phases — implementation order

Transport first, then per-surface UI, then docs + release. Each UI
surface lands in its own commit so rollback granularity matches v0.11.

### Phase 1 — `:shared` transport additions

Six new `TransportClient` methods. Additions-only (no interface
breakage). Tests mirror the v0.11 MockWebServer pattern.

1. `listSchedules(): Result<List<Schedule>>` → `GET /api/schedule`
2. `createSchedule(task: String, cron: String, enabled: Boolean = true): Result<Schedule>`
   → `POST /api/schedule`
3. `deleteSchedule(id: String): Result<Unit>` → `DELETE /api/schedule?id=<id>`
4. `browseFiles(path: String? = null): Result<FileList>` → `GET /api/files?path=<path>`
5. `listCommands(): Result<List<SavedCommand>>` → `GET /api/commands`
6. `saveCommand(name: String, command: String): Result<Unit>` → `POST /api/commands`
7. `deleteCommand(name: String): Result<Unit>` → `DELETE /api/commands?name=<name>`
8. `fetchConfig(): Result<ConfigView>` → `GET /api/config` (masked fields stay as
   sent; mobile renders `***` verbatim)

New domain types:
- `Schedule(id, task, cron, enabled, createdAt)`
- `FileEntry(name, path, isDirectory)` + `FileList(path, entries)`
- `SavedCommand(name, command)`
- `ConfigView(raw: Map<String, JsonElement>)` — permissive wrapper since the
  parent's config schema is large and version-skewable

**Tests:** MockWebServer round-trip per method (~10 new cases).

**Acceptance:** `:shared:testDebugUnitTest` green with 10 new test
functions; no UI code touched.

### Phase 2 — Schedules surface

Settings → new **Schedules** card listing every scheduled command with
per-row delete, plus a **+ New schedule** affordance opening a dialog
with:

- Task text (multi-line)
- Cron expression (with inline hint — "`0 9 * * *`" etc.)
- Enabled toggle

Session detail gets a "Schedule reply" entry in its overflow menu so
a user can turn a reply into a recurring nudge.

**Why Settings and not a new tab:** the bottom nav is already four tabs;
adding a fifth for a rarely-used feature burns density. Put it with the
other server-state cards in Settings.

### Phase 3 — File picker

Reusable `FilePickerDialog` composable:

- Top: current path breadcrumb, tap a crumb to jump up
- List: entries sorted dirs-first; `⬆ ..` parent row; tap a dir to
  descend; tap a file to select
- Bottom: **Pick this folder** button when no file is selected

Wires into two call sites:
1. `NewSessionScreen` — adds a "Working directory" field with a
   "Browse…" button (currently new sessions inherit the server's cwd).
2. `ScheduleDialog` (Phase 2) — same Browse button for the schedule's
   working dir.

### Phase 4 — Saved command library (BL20 absorbed)

Settings → new **Saved commands** card. Tap a row to expand; swipe to
delete. `+` opens a "Save command" dialog with name + command fields.

Integration with `NewSessionScreen`: a "From library" dropdown above
the task field inlines the saved command's body into the editor so the
user can tweak before starting.

Closes BL20. Moves to `Completed backlog`.

### Phase 5 — Config viewer (read-only)

Settings → new **Daemon config** card with a collapsible
key-value tree. Masked values render as `***` exactly as the parent
sends them. A subtle "Read-only — write lands in v0.13 via a
structured form per ADR-0019" footer.

### Phase 6 — Session output backlog pager UI

In `SessionDetailScreen`'s terminal toolbar (v0.11 Phase 3), add a
**Load backlog** icon to the left of the search icon. On tap: call
`transport.fetchOutput(sessionId, lines = 1000)` and prepend the text
into the xterm buffer via a new `window.dwPrependBacklog(s)` host
function. Idempotent — pressing it again is a no-op until the session
changes.

**Why prepend and not clear-then-replay:** the WebSocket stream is
already writing live events; replacing the buffer would strand any
frames that arrived during the pager call. Prepend = "here is what
came before you subscribed".

### Phase 7 — Docs + release

1. `docs/usage.md` — document all new surfaces.
2. `docs/parity-plan.md` + `docs/parity-status.md` — flip the v0.12
   rows that ship, explicitly annotate the deferred rows with their
   upstream-issue pointer.
3. Plan-doc footer → phase → commit mapping.
4. CHANGELOG `[0.12.0]` entry.
5. gradle.properties + Version.kt bump: `0.11.0 → 0.12.0`,
   versionCode `12 → 13`.
6. Live-validate every Phase 2-6 flow on the Galaxy S24 Ultra against
   a real datawatch server (same device used for v0.11 validation).
7. Tag `v0.12.0` only after user sign-off.

## Risks

| Risk | Impact | Mitigation |
|---|---|---|
| Parent `POST /api/schedule` wire shape differs from audit | Phase 2 transport mismatch | Live-call the parent with a curl before shipping the DTO; fall back to permissive `JsonElement` body if fields don't match |
| `/api/files` treats some paths as restricted | Phase 3 picker shows "access denied" | Surface the server's 403 as an inline row message, don't crash |
| Config `***` masking omits some sensitive fields that the parent hasn't flagged yet | viewer shows a real token | Apply a second client-side mask to anything matching common secret-field-name patterns (`*token*`, `*secret*`, `*key*`) before display |
| Version skew: `/api/commands` CRUD doesn't match BL20 user-intent model | Phase 4 awkwardness | Start with list + save + delete only; don't try to model categories/tags until BL21 phase 2 |

## Out of scope — explicitly

- Channels CRUD — v0.13 after upstream
- Per-session model picker — v0.13 after upstream
- Daemon log viewer / interfaces / restart — v0.13 after upstream
- Session timeline view — v0.13 after upstream
- Config write (PUT /api/config) — v0.13 per ADR-0019 structured form
- Signal device linking (BL21) — v0.13 as a dedicated sprint item
- Memory / KG panel — v0.13 (parity-plan §6 v0.13)
- eBPF per-process network view — v0.13
- Compose UI test infrastructure — separate follow-up task

## Versioning

Final release commit of Phase 7 bumps the three version locations
together. `versionCode` 12 → 13 (monotonic per AGENT.md). Tag
`v0.12.0` after live-validation sign-off.
