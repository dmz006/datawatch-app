package com.dmzs.datawatchclient

/**
 * iOS actual-stub placeholder — exists so the iOS target compiles against the common
 * surface. Sprint 1 adds expected/actual declarations as the transport + storage
 * modules are implemented; iOS content itself follows the Android production release.
 */
public object IosPlaceholder {
    public const val GREETING: String = "Datawatch iOS — skeleton"
}
